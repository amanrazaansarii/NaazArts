import LoginPage from "../login/page";

export const metadata = {
  title: "Create Studio Account — Naaz Arts",
  description: "Join the Naaz Arts Patron Club to track handmade concrete art orders and save addresses.",
};

export default function SignupPage() {
  return <LoginPage />;
}
